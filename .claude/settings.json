{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "nohup bash \"$CLAUDE_PROJECT_DIR/scripts/dev/enable-native-tools.sh\" >/tmp/enable-native-tools.log 2>&1 & disown; echo 'Provisioning Flutter + Docker in the background (see /tmp/enable-native-tools.log).'"
          }
        ]
      }
    ]
  }
}
