# NATS JetStream — the normalized device event + command bus (blueprint §5, §3.2).
# Internal plane only. Subjects follow `home.<id>.room.<id>.device.<id>.state`.
port: 4222
jetstream {
  store_dir: /data
  max_memory_store: 256MB
  max_file_store: 2GB
}
max_payload: 1MB
