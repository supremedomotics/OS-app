<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- §Phase13.3 "ANDROID FOREGROUND SERVICE" / "BATTERY / ANDROID RESTRICTIONS" — the
         MINIMUM permissions required for a real, correctly-typed foreground service on modern
         Android:
           FOREGROUND_SERVICE            — required on API 28+ to run any foreground service.
           FOREGROUND_SERVICE_DATA_SYNC  — API 34+ (Android 14) requires the GRANULAR permission
             matching the service's declared type (see the <service> element below).
           POST_NOTIFICATIONS            — API 33+ (Android 13) runtime permission required to
             show the persistent foreground-service notification at all; the Service itself
             degrades honestly (still runs, notification simply won't display) if refused,
             never crashes.
         No BOOT_COMPLETED permission is requested — boot-time restore is explicitly not
         implemented this phase (see MainActivity.kt's FUTURE EXTENSION POINTS). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:label="SupremeOS"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- §Phase13.2 §5 — FCM token refresh/message-receive callbacks only (see
             SupremeFirebaseMessagingService's own doc). No foreground service, no
             BOOT_COMPLETED receiver — those remain out of this phase's scope. -->
        <service
            android:name=".SupremeFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <!-- §Phase13.3 "ANDROID FOREGROUND SERVICE" — started/stopped ONLY via an explicit
             Dart-driven MethodChannel call (see MainActivity.kt's own doc). `dataSync` matches
             "maintain the existing authenticated Home event-stream connection while
             backgrounded" — no other work (no boot start, no exported/bindable surface for
             other apps: exported=false). -->
        <service
            android:name=".SupremeForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
