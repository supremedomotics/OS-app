package com.supremedomotics.tvagent.agent

import android.app.Application

/** UNVERIFIED. Application entry point — intentionally holds no state of its own;
 * `SupremeOsAgentService` owns the Agent's actual lifecycle. */
class SupremeOsTvAgentApplication : Application()
