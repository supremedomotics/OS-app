# dimming captures

Empty as of this session. No capture yet exercises a real dimmerChannel (control type 1) value —
`living-room/notify-control-values.json`'s controls are onOffToggle relays, not a dimmer. Reserved
for real hardware captures — see the parent directory's `README.md` for the Live Capture workflow.
Note: this is also the exact capability the still-open "RGBW/CCT capability inference" TODO item
depends on real hardware to resolve — a real dimming capture would help close both gaps at once.
