# sensor-events captures

Empty as of this session. `living-room/notify-control-values.json` includes a real presence/lux
sensor reading as PART of a broader capture, but no capture here isolates a sensor-only update
(e.g. a real battery, temperature, or presence transition on its own). Reserved for real hardware
captures — see the parent directory's `README.md` for the Live Capture workflow.
