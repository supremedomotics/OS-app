# button-events captures

Empty as of this session. `kitchen/button-press.json` already exercises the button-event opcode
(0x51) but is synthetic — this directory is reserved for REAL button-press captures gathered from
real hardware. See the parent directory's `README.md` for the Live Capture workflow to populate it.
