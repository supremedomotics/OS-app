{
  "name": "@supreme/tools-loadtest",
  "version": "0.0.0",
  "private": true,
  "description": "Load / soak / chaos harness (§5/§15) — drives the real gateway and measures latency, throughput, errors, memory, and recovery.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "run": "tsx src/run.ts",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@supreme/gateway": "workspace:*",
    "@supreme/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "fastify": "^5.2.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
